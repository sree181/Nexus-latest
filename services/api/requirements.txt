fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
networkx==3.4.2          # hgviz: bipartite decomposition, cycle basis, Betti numbers
httpx==0.27.2            # real PyPI version + OSV advisory lookups
openai==1.51.0           # model-built runs; unused until OPENAI_API_KEY is set
pyjwt[crypto]==2.9.0     # OIDC: verify the IdP's signature on an access token
# When wiring the real engine, add your HyperMesh wheel here, e.g.:
# hypermeshdb @ file:///opt/wheels/hypermeshdb-*.whl
